<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.teaching.system.mapper.WxBusinessMapper">
    
    <resultMap type="WxBusiness" id="WxBusinessResult">
        <result property="businessId"    column="business_id"    />
        <result property="businessCode"    column="business_code"    />
        <result property="businessName"    column="business_name"    />
        <result property="status"    column="status"    />
        <result property="createBy"    column="create_by"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateBy"    column="update_by"    />
        <result property="updateTime"    column="update_time"    />
        <result property="remark"    column="remark"    />
    </resultMap>

    <sql id="selectWxBusinessVo">
        select business_id, business_code, business_name, status, create_by, create_time, update_by, update_time, remark from wx_business
    </sql>

    <select id="selectWxBusinessList" parameterType="WxBusiness" resultMap="WxBusinessResult">
        <include refid="selectWxBusinessVo"/>
        <where>  
            <if test="businessCode != null  and businessCode != ''"> and business_code = #{businessCode}</if>
            <if test="businessName != null  and businessName != ''"> and business_name like concat('%', #{businessName}, '%')</if>
            <if test="status != null  and status != ''"> and status = #{status}</if>
        </where>
    </select>
    
    <select id="selectWxBusinessByBusinessId" parameterType="Long" resultMap="WxBusinessResult">
        <include refid="selectWxBusinessVo"/>
        where business_id = #{businessId}
    </select>

    <insert id="insertWxBusiness" parameterType="WxBusiness" useGeneratedKeys="true" keyProperty="businessId">
        insert into wx_business
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="businessCode != null and businessCode != ''">business_code,</if>
            <if test="businessName != null and businessName != ''">business_name,</if>
            <if test="status != null">status,</if>
            <if test="createBy != null">create_by,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateBy != null">update_by,</if>
            <if test="updateTime != null">update_time,</if>
            <if test="remark != null">remark,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="businessCode != null and businessCode != ''">#{businessCode},</if>
            <if test="businessName != null and businessName != ''">#{businessName},</if>
            <if test="status != null">#{status},</if>
            <if test="createBy != null">#{createBy},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateBy != null">#{updateBy},</if>
            <if test="updateTime != null">#{updateTime},</if>
            <if test="remark != null">#{remark},</if>
         </trim>
    </insert>

    <update id="updateWxBusiness" parameterType="WxBusiness">
        update wx_business
        <trim prefix="SET" suffixOverrides=",">
            <if test="businessCode != null and businessCode != ''">business_code = #{businessCode},</if>
            <if test="businessName != null and businessName != ''">business_name = #{businessName},</if>
            <if test="status != null">status = #{status},</if>
            <if test="createBy != null">create_by = #{createBy},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="remark != null">remark = #{remark},</if>
        </trim>
        where business_id = #{businessId}
    </update>

    <delete id="deleteWxBusinessByBusinessId" parameterType="Long">
        delete from wx_business where business_id = #{businessId}
    </delete>

    <delete id="deleteWxBusinessByBusinessIds" parameterType="String">
        delete from wx_business where business_id in 
        <foreach item="businessId" collection="array" open="(" separator="," close=")">
            #{businessId}
        </foreach>
    </delete>
</mapper>